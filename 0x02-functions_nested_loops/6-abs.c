#include "main.h"

/**
 *  _abs - retrun the abs value
 * @n:number being passed around
 * Return: positive either way
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
